<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Online Journal</h1>
    <ul> 
        <% for(let i = 0; i < zebra.length; i++){ %>
            <li class="todoItem">
                <% if(zebra[i].completed === true) {%>
                    <span class="completed"><%= zebra[i].todo %></span>
                    <% }else{ %>
                    <span><%= zebra[i].todo %></span>
                    <% } %>   
                    <span class='del'><button> delete </button></span>
                    <span class="feed"><button> add to feed </button></span>
                </li>
            <% } %>    
            </ul>



    <form action="createTodo" method="POST">
        <input type="date" name="enter date" min="2020-01-01" max="2030-12-31" >
        <input type="text" placeholder ="enter memory" name="todoItem">
        <input type="submit">
    </form>
    <script src="/js/main.js"></script>
</body>
</html>